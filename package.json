{
  "name": "todo-app",
  "version": "1.0.0",
  "description": "Full-stack To-Do app — React + REST API layer + Persistent Storage",
  "scripts": {
    "dev": "concurrently \"vite\" \"node backend/server.mjs\"",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "concurrently": "^9.2.1",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^4.4.0"
  }
}
